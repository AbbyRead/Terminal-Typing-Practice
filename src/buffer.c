#include <stdio.h>
#include <stdlib.h>

#include "buffer.h"

char *create_buffer(FILE *source_text) {
	char *buffer = NULL;


	return buffer;
}

int count_lines(const char *buffer) {
	int lines = 0;


	return lines;
}
